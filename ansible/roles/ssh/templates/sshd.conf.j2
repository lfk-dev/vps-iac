Port {{ vault_vps_ssh_port | default(22) }}
PermitRootLogin no
X11Forwarding no
MaxAuthTries 3
PasswordAuthentication {{ ssh_password_auth | default('yes') }}
PubkeyAuthentication yes

# The hardening is minimal to prevent missconfiguration and lockouts.
